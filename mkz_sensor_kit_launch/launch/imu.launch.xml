<launch>
  <!-- Do NOT launch the NovAtel driver here -->
  <arg name="launch_driver" default="false"/>

  <!-- Raw IMU topic from the NovAtel driver -->
  <arg name="imu_raw_name" default="/novatel/oem7/imu/data"/>

  <!-- IMU corrector params (point this to your tuned file if you have one) -->
  <arg name="imu_corrector_param_file"
       default="$(find-pkg-share individual_params)/config/$(var vehicle_id)/sample_sensor_kit/imu_corrector.param.yaml"/>

  <!-- Toggle gyro bias estimator (true by default) -->
  <arg name="launch_gyro_bias_estimator" default="true"/>

  <group>
    <push-ros-namespace namespace="imu"/>

    <!-- IMU Corrector: publishes /sensing/imu/imu_data -->
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/imu_corrector.launch.xml">
      <arg name="input_topic" value="$(var imu_raw_name)"/>
      <arg name="output_topic" value="imu_data"/>
      <arg name="param_file" value="$(var imu_corrector_param_file)"/>
    </include>

    <!-- Gyro bias estimator (subscriber to twist; harmless to keep; toggleable) -->
    <group if="$(var launch_gyro_bias_estimator)">
      <include file="$(find-pkg-share autoware_imu_corrector)/launch/gyro_bias_estimator.launch.xml">
        <arg name="input_imu_raw" value="$(var imu_raw_name)"/>
        <arg name="input_twist" value="/sensing/vehicle_velocity_converter/twist_with_covariance"/>
        <arg name="imu_corrector_param_file" value="$(var imu_corrector_param_file)"/>
      </include>
    </group>
  </group>
</launch>

